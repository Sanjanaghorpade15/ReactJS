function Chai(){
    return(
        //If there is any HTML tag in return statement then we will user file name as .jsx and if we are returning only JS then we'll use .js
        <h2>Chai in react</h2>
    )
}


export default Chai;